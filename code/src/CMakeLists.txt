# This is just a pitstop on out way down to the source. Nothing special happening # here.

# Make sure to keep the order straight. Things that are independent first, follow # the dependency order afterwards.
add_subdirectory(library)
add_subdirectory(app)
